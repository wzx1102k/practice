#include <Magick++.h>
#include <string>
#include <iostream>
#include <list>

using namespace std;

using namespace Magick;


int main(int argc, char** argv)
{
	InitializeMagick(*argv);
//	Image image;

/*	try {
		cout<<"ehl"<<endl;
		//image.read("ocr1.jpg");
	//	image.resize(Geometry(100, 100));
	//	image.write("cloud.jpg");
	}

	catch(Exception &error_)
	{
		cout << "Caught exception: "<< error_.what() << endl;
		return 1;
	}*/
	return 0;
}
